export const sp = {
  // Block components default to itemToItem internally.
  // Use this only to override (e.g. mt-0 inside a grid, or extra space when needed).
  itemToItem: "mt-6 md:mt-8",

  // SectionHeading applies this as mb internally.
  // Exported for reference or manual override.
  sectionToContent: "mb-8 md:mb-10",
}
