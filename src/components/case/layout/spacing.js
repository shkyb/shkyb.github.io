export const sp = {
  // After a SectionHeading, before the first content block
  sectionToContent: "mt-8 md:mt-10",

  // Between sibling content blocks (e.g. Figure → Figure, ProcessStep → ProcessStep)
  itemToItem: "mt-6 md:mt-8",
}
